# FitForge Agent Environment Configuration (Milestone 1)
# Note: Milestone 1 runs entirely in local deterministic mock mode.
# No external APIs or credentials are required.

ENVIRONMENT=development
PORT=8000
HOST=127.0.0.1
LOG_LEVEL=info
